#include<stdio.h>
void mydisplay(void)
{
	cout << "\nDisplaying Information: " << endl;

	// Displaying information
	for(int i = 0; i < 10; ++i)
		{
		    printf("\nName: ");
    		    puts(s.name);

    		    printf("Roll number: %d\n",s.roll);

    		    printf("Marks: %.1f\n", s.marks);

		}
	return
}
